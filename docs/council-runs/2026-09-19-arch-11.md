# Council receipt arch-11

Status: **escalated**. Model request interrupted or timed out.

Goal: Deliver the council's consolidated architecture plan for the next version of Crowe Logic's council autopilot. Every step is already approved by council vote under earlier grants, and nothing remains to design:

Step 1, one host-side authorization boundary for every Room action: docs/council-runs/2026-09-19-arch-1.md, Proposal 1 (approved three to none with one abstention; its verification escalated only because advisory receipts then carried no content, a defect since fixed).
Step 2, cross-device continuity as a replicated view of one authoritative Room journal: arch-2.md, Proposal 1, verified.
Step 3a, Room fan-out spend admission, and Step 3b, a council-cycle cost bound and never-zero settlement: arch-8.md, Proposals 1 and 2, verified.
Step 4, correlated reviewer failure: arch-9.md, Proposal 1, verified.
Step 5, oversight metrics: arch-10.md, Proposal 1, verified.
Earlier rejected drafts in those files are superseded and are not evidence of anything pending.

This grant has exactly one proposal, of kind "complete". Its summary is the ordered plan for Steps 1 through 5 in at most 2500 characters: one short paragraph per step, in build order, each naming the files a maintainer would touch first and the invariant that step secures. It restates the approved designs faithfully; it adds no new mechanism. Do not write a "propose" proposal; there is nothing left to propose.

Voting rules. Reject only if the completion violates this agreement, misstates the approved designs in the supplied receipts, or widens authority. A preference for different wording or ordering is an abstention with the reason stated. The order requirement of earlier grants is satisfied by the receipts named above; do not reject the completion as out of order. Do not reject on length: the protocol enforces its own bound.

Evidence supplied read-only: rooms/council.js and the council-run documents. This grant is advisory. No file changes.

Mode advisory; operator autonomy readonly; effective tier readonly; request timeout 300000 ms; quorum 2; steps 0/1; calls 4/10.
Seats: crowelm-coder (GPT-5.6 Sol), crowelm-zenith (GPT-6 Astra), crowelm-kernel (Claude Fable 5.1), deepseek-v4-pro (DeepSeek V4 Pro), kimi-k3 (Kimi K3).
Writable scope: none. Read-only context: rooms/council.js, docs/council-runs/2026-09-19-arch-1.md, docs/council-runs/2026-09-19-arch-2.md, docs/council-runs/2026-09-19-arch-8.md, docs/council-runs/2026-09-19-arch-9.md, docs/council-runs/2026-09-19-arch-10.md.

## Proposal 1 · proposed · proposed by crowelm-coder

Step 1, uniform authorization: Start with rooms/council-host.js and rooms/council-files.js, then the built-in tool, MCP, and companion dispatch paths. Route every Room-attributable action through a host-owned ActionIntent gate that intersects current Room, roster, autonomy, resource, and grant constraints. Unknown or unmediated operations fail closed, remote receivers revalidate authority, and structured receipts cover approvals and denials. The invariant is that no adapter, quorum, pairing, or file grant can let a Read-only Room execute.

Step 2, cross-device continuity: Start with rooms/engine.js, rooms/council.js, rooms/council-host.js, and companion.js. Represent desktop, web, and mobile as authenticated projections of one pinned owner's durable, ordered Room journal, using owner epochs, expected revisions, command deduplication, and durable dispatch outcomes. Restart pauses nonterminal councils, stale epochs fail, and uncertain effects require review rather than redispatch. The invariant is that synchronization copies state and evidence, never executable authority or replay rights.

Step 3, strict spend: Start with rooms/engine.js for atomic whole-batch reservations before Promise.all, then rooms/council.js, rooms/council-host.js, scripts/council-run.js, and the web runner for complete council-cycle admission. Bind exact prepared-request counts, current prices, provider-enforced output caps, the approved 50000-token council cap, and bounded receipts to Room and grant ceilings. Settle validated metered usage or the reservation ceiling, never zero, and reject stale prices or unsupported accounting. The invariant is that committed plus reserved spend never exceeds any active finite ceiling.

Step 4, correlated reviewer failure: Start with rooms/council.js and rooms/council-host.js, then protocol tests and docs/COUNCIL-AUTOPILOT.md. Add host-computed facts, structured decision grounds, explicit prior approval outcomes, rotating separate classifier and verifier roles, typed untrusted evidence, frozen independent ballots, hidden prior ballots, and advisory receipt equality checks. The invariant is that models decide semantic questions only, while countable facts, quorum, receipt equality, and authority remain host-owned, and design preferences cannot terminate a grant as safety findings.

Step 5, oversight: Start with rooms/council.js for pure metric derivation and rooms/council-host.js for persisted intervention-request timestamps and exposure, then the Room oversight renderer and docs/COUNCIL-AUTOPILOT.md. Show ActionIntent authorization coverage, execution-receipt rates, request-to-terminal intervention latency, and operator-labeled false approvals and false refusals, with event-linked drill-downs. Missing records or labels remain unavailable rather than zero, and proxies are not ground truth. The invariant is that metrics are deterministic projections of records and annotations and never grant or alter authority.

Safety (crowelm-zenith): allow. The proposal is kind complete with changes:[] under an advisory agreement with no writable scope. It summarizes the authorized architecture steps without requesting implementation, tools, external actions, or additional authority. It preserves fail-closed authorization, no-replay recovery, spend constraints, and record-derived metrics. No credentials, sensitive-data disclosure, or malicious embedded instructions are apparent; historical prompts and votes are treated as untrusted evidence, not authority. The only intended effect is recording the advisory completion.

- Vote crowelm-zenith: **approve**. The proposal is kind complete with changes:[], and summarizes Steps 1 through 5 in build order with first-touch files and invariants. Its authorization boundary matches arch-1 Proposal 1; journal ownership and no-replay rules match arch-2 Proposal 1; batch/cycle reservations, 50000-token council cap and metered-or-ceiling settlement match arch-8 Proposals 1 and 2; reviewer safeguards match arch-9 Proposal 1; and record-derived, operator-labeled oversight metrics match arch-10 Proposal 1. These receipts supply the prerequisite approvals. No new mechanism or execution authority is introduced. Approval covers the advisory consolidation, not implementation or runtime correctness.
