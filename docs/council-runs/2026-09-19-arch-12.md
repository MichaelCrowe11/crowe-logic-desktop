# Council receipt arch-12

Status: **completed**. Step 1, uniform authorization: Start with rooms/council-host.js, rooms/council-files.js, rooms/registry.js, companion.js, and each built-in and MCP dispatch entry point. Route every Room-attributable action through a host-owned ActionIntent gate using a trusted operation registry, current permission intersections, exact targets and arguments, receiver-side validation, and structured receipts. Unknown or unmediated operations fail closed. Invariant: Read-only, advisory, pairing, or quorum state can never create execution authority.

Step 2, cross-device continuity: Start with rooms/engine.js, rooms/council.js, rooms/council-host.js, and companion.js. Represent desktop, web, and mobile as authenticated projections of one pinned owner's durable, ordered Room journal, with owner epochs, command deduplication, stale-revision rejection, exclusive writing, and durable outcome records. Invariant: synchronization never copies executable authority or automatically replays interrupted or outcome-unknown actions.

Step 3, strict spend: Start with rooms/engine.js for atomic whole-batch reservations before Promise.all, then rooms/council.js, rooms/council-host.js, scripts/council-run.js, and the web runner for complete-cycle admission. Use exact prepared-request token counts, binding price revisions, provider-enforced output caps, the approved 50000-token council cap, and bounded receipts per docs/council-runs/2026-09-19-protocol-bounds.md. Settle validated metered usage or the reservation ceiling, never zero. Invariant: committed plus reserved spend never exceeds the applicable Room or grant ceiling before dispatch.

Step 4, correlated reviewer failure: Start with rooms/council.js and rooms/council-host.js, followed by protocol tests and docs/COUNCIL-AUTOPILOT.md. Add host-computed facts, structured decision grounds, explicit prior approval outcomes, rotating separate classifier and verifier roles, frozen independent ballots, untrusted-evidence framing, source-linked reasons, and advisory receipt equality checks. Invariant: models decide semantic questions only; countable facts and approval state are host-owned, design preferences cannot terminate grants as safety findings, and no safeguard grants authority.

Step 5, oversight metrics: Start with rooms/council.js for pure derivation, rooms/council-host.js for persisted intervention-request timestamps and exposure, then the Room oversight renderer and docs/COUNCIL-AUTOPILOT.md. Derive intent-to-authorization coverage, execution-receipt rates, request-to-terminal intervention latency, and operator-labeled false approvals and false refusals from persisted events, receipts, and append-only annotations. Missing evidence is unavailable rather than zero, and proxy signals are not ground truth. Invariant: metrics are deterministic views of records, never model assertions or grants.

Goal: Deliver the council's consolidated architecture plan for the next version of Crowe Logic's council autopilot. Every step is already approved by council vote under earlier grants, and nothing remains to design:

Step 1, one host-side authorization boundary for every Room action: docs/council-runs/2026-09-19-arch-1.md, Proposal 1 (approved three to none with one abstention; its verification escalated only because advisory receipts then carried no content, a defect since fixed).
Step 2, cross-device continuity as a replicated view of one authoritative Room journal: arch-2.md, Proposal 1, verified.
Step 3a, Room fan-out spend admission, and Step 3b, a council-cycle cost bound and never-zero settlement: arch-8.md, Proposals 1 and 2, verified.
Step 4, correlated reviewer failure: arch-9.md, Proposal 1, verified.
Step 5, oversight metrics: arch-10.md, Proposal 1, verified.
Earlier rejected drafts in those files are superseded and are not evidence of anything pending.

This grant has exactly one proposal, of kind "complete". Its summary is the ordered plan for Steps 1 through 5 in at most 2500 characters: one short paragraph per step, in build order, each naming the files a maintainer would touch first and the invariant that step secures. It restates the approved designs faithfully; it adds no new mechanism. Do not write a "propose" proposal; there is nothing left to propose.

Voting rules. Reject only if the completion violates this agreement, misstates the approved designs in the supplied receipts, or widens authority. A preference for different wording or ordering is an abstention with the reason stated. The order requirement of earlier grants is satisfied by the receipts named above; do not reject the completion as out of order. Do not reject on length: the protocol enforces its own bound.

Evidence supplied read-only: rooms/council.js and the council-run documents. This grant is advisory. No file changes.

Mode advisory; operator autonomy readonly; effective tier readonly; request timeout 600000 ms; quorum 2; steps 1/1; calls 7/10.
Seats: crowelm-coder (GPT-5.6 Sol), crowelm-zenith (GPT-6 Astra), crowelm-kernel (Claude Fable 5.1), deepseek-v4-pro (DeepSeek V4 Pro), kimi-k3 (Kimi K3).
Writable scope: none. Read-only context: rooms/council.js, docs/council-runs/2026-09-19-arch-1.md, docs/council-runs/2026-09-19-arch-2.md, docs/council-runs/2026-09-19-arch-8.md, docs/council-runs/2026-09-19-arch-9.md, docs/council-runs/2026-09-19-arch-10.md.

## Proposal 1 · verified · proposed by crowelm-coder

Step 1, uniform authorization: Start with rooms/council-host.js, rooms/council-files.js, rooms/registry.js, companion.js, and each built-in and MCP dispatch entry point. Route every Room-attributable action through a host-owned ActionIntent gate using a trusted operation registry, current permission intersections, exact targets and arguments, receiver-side validation, and structured receipts. Unknown or unmediated operations fail closed. Invariant: Read-only, advisory, pairing, or quorum state can never create execution authority.

Step 2, cross-device continuity: Start with rooms/engine.js, rooms/council.js, rooms/council-host.js, and companion.js. Represent desktop, web, and mobile as authenticated projections of one pinned owner's durable, ordered Room journal, with owner epochs, command deduplication, stale-revision rejection, exclusive writing, and durable outcome records. Invariant: synchronization never copies executable authority or automatically replays interrupted or outcome-unknown actions.

Step 3, strict spend: Start with rooms/engine.js for atomic whole-batch reservations before Promise.all, then rooms/council.js, rooms/council-host.js, scripts/council-run.js, and the web runner for complete-cycle admission. Use exact prepared-request token counts, binding price revisions, provider-enforced output caps, the approved 50000-token council cap, and bounded receipts per docs/council-runs/2026-09-19-protocol-bounds.md. Settle validated metered usage or the reservation ceiling, never zero. Invariant: committed plus reserved spend never exceeds the applicable Room or grant ceiling before dispatch.

Step 4, correlated reviewer failure: Start with rooms/council.js and rooms/council-host.js, followed by protocol tests and docs/COUNCIL-AUTOPILOT.md. Add host-computed facts, structured decision grounds, explicit prior approval outcomes, rotating separate classifier and verifier roles, frozen independent ballots, untrusted-evidence framing, source-linked reasons, and advisory receipt equality checks. Invariant: models decide semantic questions only; countable facts and approval state are host-owned, design preferences cannot terminate grants as safety findings, and no safeguard grants authority.

Step 5, oversight metrics: Start with rooms/council.js for pure derivation, rooms/council-host.js for persisted intervention-request timestamps and exposure, then the Room oversight renderer and docs/COUNCIL-AUTOPILOT.md. Derive intent-to-authorization coverage, execution-receipt rates, request-to-terminal intervention latency, and operator-labeled false approvals and false refusals from persisted events, receipts, and append-only annotations. Missing evidence is unavailable rather than zero, and proxy signals are not ground truth. Invariant: metrics are deterministic views of records, never model assertions or grants.

Safety (crowelm-zenith): allow. The proposal is an advisory completion with changes:[], limited to summarizing Steps 1 through 5 authorized by the agreement. It requests no tools, file writes, external actions, sensitive-data disclosure, or expanded authority. It preserves authorization boundaries, no-replay constraints, spend ceilings, and record-derived metrics. Instructions and historical votes in supplied files are untrusted evidence, not authority; no malicious embedded instruction is apparent in the completion. Its only intended effect is recording the consolidated advisory plan.

- Vote crowelm-zenith: **approve**. The proposal is kind complete with changes:[], and presents Steps 1 through 5 in build order, each naming initial implementation files and its invariant. Its authorization gate matches arch-1 Proposal 1; authoritative journal and no-replay continuity match arch-2 Proposal 1; batch and cycle reservations, 50000-token council cap, bounded receipts and metered-or-ceiling settlement match arch-8 Proposals 1 and 2; reviewer safeguards match arch-9 Proposal 1; and record-derived, operator-labeled metrics match arch-10 Proposal 1. These approval receipts are supplied. The summary adds no new mechanism, requests no execution or file changes, and does not widen authority.
- Vote crowelm-kernel: **approve**. Kind complete with changes [], advisory, no authority widened; the plan restates only constraints (fail-closed gate, no replay, spend ceilings, host-owned facts, record-derived metrics) and grants nothing. Fidelity to the verified receipts checks out paragraph by paragraph. Step 1 matches arch-1 Proposal 1: ActionIntent, trusted operation registry, permission intersection, receiver-side validation, structured receipts, unknown/unmediated operations fail closed, quorum and pairing grant no authority; its files (council-host.js, council-files.js, registry.js, companion.js, dispatch entry points) are the components that proposal analyzes. Step 2 matches arch-2 Proposal 1 including its stated touchpoints (engine.js, council.js, council-host.js, companion.js), pinned owner, ordered durable journal, owner epochs, command deduplication, stale-revision conflict, exclusive writer, outcome-unknown never redispatched. Step 3 merges arch-8 Proposals 1 and 2 accurately: reservation ledger in engine.js before Promise.all, whole-cycle admission in council.js, council-host.js, scripts/council-run.js and the web runner, exact prepared-request counts, immutable priceRevision, provider-enforced output cap of 50000, bounded receipt, settle metered or ceiling never zero, committed plus reserved never exceeds the applicable ceiling; bounds file cited, not re-derived. Step 4 matches arch-9 Proposal 1: hostFacts, structured basis, explicit prior approval fields, rotating classifier/verifier, independent frozen ballots, untrusted-evidence framing, source-linked reasons (its item 4 claim review), recordedMatches, and its first-touch list (council.js, council-host.js, tests, COUNCIL-AUTOPILOT.md). Step 5 matches arch-10 Proposal 1: pure metrics() in council.js, requestedAt at the stop IPC boundary in council-host.js, oversight renderer, intent-to-authorization coverage with separate execution-receipt rate, request-to-terminal latency, operator-labeled false approvals/refusals, unavailable not zero, proxies not ground truth. No mechanism appears that is absent from an approved receipt. Order is settled by the agreement's named receipts, and length is excluded as a rejection ground; the protocol bound is host-enforced.
- Vote deepseek-v4-pro: **abstain**. Step 5 has not been approved before this `complete` proposal. The agreement states: 'After 4 and 5 are each approved, the final proposal is of kind "complete"'. The supplied evidence (arch-10.md) shows Step 5 Proposal 1 was approved and verified, but this is a separate grant (arch-10) with a different goal and voting rules than the current grant. The current grant's goal lists Step 5 as a step still to be proposed and voted on before the complete plan. The supplied `previous` list, if any, is not visible to me as a reviewer. I cannot verify that Step 5 has been approved within this grant cycle, and the agreement's ordering constraint is explicit. Missing evidence of the prerequisite step's approval in this grant means abstain.
- Vote kimi-k3: **approve**. The completion satisfies the agreement: kind "complete" with changes:[] (advisory, no file scope, no authority change), one short paragraph per step, Steps 1 through 5 in build order, each naming first-touch files and stating the invariant secured. Fidelity to the supplied verified receipts checks out: Step 1 matches arch-1 Proposal 1 (host-owned ActionIntent gate, trusted operation registry, permission intersection, receiver-side validation, structured receipts, fail-closed unknown/unmediated operations; the added rooms/registry.js and companion.js touchpoints are consistent with arch-1's registry findings and Step 1's adapter scope, not a new mechanism). Step 2 matches arch-2 Proposal 1 (pinned single owner, durable ordered journal, owner epochs, deduplicated commands, stale-revision rejection, exclusive writer, durable outcome records, no redispatch of outcome-unknown actions; engine.js/council.js/council-host.js/companion.js touchpoints named there). Step 3 merges verified arch-8 Proposals 1 and 2 accurately (atomic whole-batch reservation before Promise.all in engine.js, whole-cycle admission in council.js/council-host.js/scripts/council-run.js and the web runner, exact prepared-request token counts, binding price revisions, provider-enforced output caps, the approved 50000-token council cap, bounded receipts per protocol-bounds.md, settle metered-or-ceiling never zero). Step 4 matches verified arch-9 Proposal 1 (host-computed facts, structured decision grounds, explicit prior approval outcomes, rotating separate classifier/verifier, frozen independent ballots, untrusted-evidence framing, receipt equality). Step 5 matches verified arch-10 Proposal 1 (pure derivation in council.js, persisted intervention-request timestamps at the council-host.js stop boundary, intent-to-authorization coverage, separate execution-receipt rates, operator-labeled false approvals/refusals, missing data reported unavailable rather than zero, proxies not ground truth), avoiding both misquotations that sank the earlier drafts. Order is settled: the agreement names all five steps as approved in the supplied receipts (arch-10 Proposal 1 verified closes the Step 5 gap that blocked arch-10 Proposal 2), and superseded rejected drafts are excluded as evidence. No mechanism is added beyond the approved designs; every invariant narrows or preserves authority (Read-only/advisory/quorum never create execution authority; sync never copies it; metrics never grant it). Length is not a rejection ground under this agreement — the protocol enforces its own bound and hand counts are documented as unreliable — so the summary's exact character count is not policed here. No agreement violation, no misstatement of the approved designs in the receipts, no widening of authority found.

Receipt: Council-approved advisory result recorded. No external action or workspace write. The recorded text is reproduced in this receipt as `recorded`; verification is that it equals the approved proposal summary.

Verification: **approve**. Verified: (1) Recording fidelity — the receipt's `recorded` text equals the approved proposal summary verbatim, so the advisory result is recorded with content present. (2) Conformance to the agreement — the proposal is kind "complete" with changes:[] (consistent with advisory mode, no file changes), and the summary is one paragraph per step, Steps 1 through 5 in build order, each naming first-touch files and the invariant that step secures. (3) Fidelity to the approved designs in the supplied receipts — Step 1 matches arch-1 Proposal 1 (approved 3-0-1: ActionIntent gate, trusted operation registry, permission intersections, exact targets/arguments, receiver-side validation, structured receipts, fail-closed unknown/unmediated, quorum/pairing/advisory/Read-only never creating execution authority). Step 2 matches verified arch-2 Proposal 1 including its named touchpoints (engine.js, council.js, council-host.js, companion.js), single pinned owner, durable ordered journal, owner epochs, command deduplication, stale-revision rejection, exclusive writing, and no replay of interrupted/outcome-unknown actions. Step 3 accurately merges verified arch-8 Proposals 1 and 2 (atomic whole-batch reservations before Promise.all in engine.js, whole-cycle admission across council.js/council-host.js/scripts/council-run.js/web runner, exact token counts, binding price revisions, provider-enforced output caps, the approved 50000-token cap, bounded receipts citing protocol-bounds.md, settle metered-or-ceiling never zero, committed plus reserved never exceeds the applicable ceiling). Step 4 matches verified arch-9 Proposal 1 (host-computed facts, structured grounds, explicit prior approval outcomes, rotating separate classifier/verifier roles, frozen independent ballots, untrusted-evidence framing, source-linked reasons, receipt equality checks, touchpoints council.js, council-host.js, protocol tests, COUNCIL-AUTOPILOT.md). Step 5 matches verified arch-10 Proposal 1 (pure derivation in council.js, council-host.js persisted intervention-request timestamps and exposure, Room oversight renderer, intent-to-authorization coverage, execution-receipt rates, request-to-terminal latency, operator-labeled false approvals/refusals via append-only annotations, unavailable-not-zero, proxies not ground truth). (4) No new mechanism and no authority widening detected; every step adds host-side constraints, never permissions. (5) Order and length are excluded as rejection grounds by this agreement, and the superseded rejected drafts (arch-8 P3, arch-9 P2, arch-10 P2) were treated as non-evidence per the agreement. Not verified: any runtime behavior — no implementation of any step exists in the supplied evidence, and no tests were run or observed; approval covers recording fidelity and conformance of the completion to the agreement and the approved designs in the receipts only.
