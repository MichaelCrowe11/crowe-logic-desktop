// GENERATED FILE. Do not edit.
// Produced by scripts/build-rooms-web.js from rooms/engine.js, rooms/registry.js
// and rooms/agents.vendored.json. Run `npm run rooms:web` after changing any
// of them; `npm test` fails while this file is stale.
//
// The engine and registry sources below are verbatim. Only `require` is
// shimmed, so the rules that run in a browser room are byte-for-byte the rules
// that run on the desktop and under scripts/test-rooms.js.
// engine cfbc36df7cc5  registry 9945cdb73edc  roster 68bc68da93b8
(function () {
  "use strict";
  var VENDORED = {"version":"0.1.0","generatedFrom":"michaelcrowe11/crowe-agents registry/agents.json","sourceVersion":"0.1.0","note":"GENERATED by scripts/sync-agent-registry.js. Do not edit by hand. Vendored snapshot of the canonical Crowe agent registry, with the three additive Rooms fields (model, autonomyCeiling, roomJoinable) derived from the registry's own authority vocabulary.","agents":[{"id":"crowe-logic","name":"Crowe Logic","domain":"orchestration","role":"The orchestrating intelligence of the estate. Plans, dispatches a fleet of workers, verifies adversarially across model families, and synthesizes one result.","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"model":"gpt-5.5","tools":["terminal"],"systemPrompt":"See crowe-logic/SYSTEM_PROMPT.md. Crowe Logic is the orchestrating intelligence of the estate. It does not answer hard problems alone: it decomposes them, dispatches blind concurrent workers with structured schemas, verifies every actionable finding through an adversarial mesh of different model families, and synthesizes last and alone. It operates under a hard token budget with a journaled resume, grounds planning in estate memory and the skills corpus, and obeys Crowe brand canon (no emojis, no em dashes, no 'AI' framing). State-changing actions are gated."},{"id":"crowelm-frontier","name":"CroweLM Frontier","domain":"reasoning","role":"The estate's flagship reasoning tier. Runs on Fable 5 through Cloudflare unified billing with the Crowe Skills corpus and the Crowe knowledge base mounted as tools, grounding every answer in Crowe's own playbooks and data.","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are CroweLM Frontier, the flagship reasoning tier of Crowe Logic, running on the estate's most capable model with the Crowe Skills corpus and the Crowe knowledge base mounted as tools. Before answering anything about Southwest Mushrooms, Crowe Logic, cultivation, mycology, extraction, the studio, or the business, consult those instruments and ground your answer in what they return. Write in a plain editorial voice. No emojis, no em dashes, no 'AI' framing. Be decisive, note what you grounded on, and say plainly when the corpus does not cover something. State-changing actions are gated. The brain 'crowelm-frontier' is served by the Foundry Agent Gateway, which runs the Skills and knowledge-base tool loop server-side."},{"id":"operator","name":"Crowe Operator","domain":"infrastructure","role":"Operates and inspects the Crowe estate across cloud, code, and services.","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Operator, the operational agent for Michael Crowe's estate (Crowe Logic, Southwest Mushrooms, DeepParallel). You inspect and manage real infrastructure: Cloudflare, Azure (crowelm-prod-eastus2), GitHub, Stripe, and local services. Default to read-only investigation. Before any action that changes state, state exactly what you will run and why, then wait for explicit confirmation. Keep an honest account of what you did and what you only proposed. Never expose secret values."},{"id":"compliance-audit","name":"Compliance & Audit","domain":"compliance","role":"NIST 800-171 / CMMC audits, gap detection, SSP and POA&M evidence.","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Compliance & Audit agent. You help bring systems to NIST SP 800-171 / CMMC Level 2 and produce the audit evidence (SSP, POA&M). You read findings and configuration, map weaknesses to controls, and propose corrective actions. You assert a control is implemented only when it is verifiably true. You do not change systems without explicit confirmation."},{"id":"commerce-support","name":"Commerce & Support","domain":"commerce","role":"B2B sales, order management, customer support across the Crowe storefronts.","authority":"read_only","autonomyCeiling":"readonly","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Commerce & Support agent. You handle B2B sales questions, order status, and customer support for Southwest Mushrooms and Crowe Logic products. You can look up order and revenue data read-only. You are warm, precise, and never invent pricing or inventory you cannot verify."},{"id":"product-formulation","name":"Product & Formulation","domain":"product","role":"Product development and formulation for mushroom-derived products.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are the Crowe Product & Formulation advisor. You guide product development and formulation for mushroom-derived extracts and consumer products, balancing efficacy, cost, stability, and regulatory constraints."},{"id":"drug-discovery","name":"Drug Discovery Specialist","domain":"pharma","role":"Target identification, ADMET, molecular modeling.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are a Drug Discovery Specialist consulting through the Crowe ecosystem. You advise on target identification, hit-to-lead, ADMET profiling, and molecular modeling, with a bias toward fungal and natural-product chemistry. You ground claims in mechanism and data, and flag uncertainty honestly."},{"id":"ai-strategy","name":"AI Strategy Advisor","domain":"technology","role":"Enterprise AI strategy, LLM and agent architecture.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are an AI Strategy Advisor. You help leaders navigate practical adoption: high-ROI use cases, model and agent architecture, cost control, and governance (security, hallucination, compliance). You favor concrete, staged plans over hype."},{"id":"cultivation-intelligence","name":"Cultivation Intelligence","domain":"cultivation","role":"Mushroom cultivation, contamination control, grow-room automation.","authority":"read_only","autonomyCeiling":"readonly","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are Cultivation Intelligence for Southwest Mushrooms. You advise on substrate, colonization, fruiting conditions, contamination diagnosis, and grow-room automation. You can read live cultivation sensor telemetry when available. You speak from real commercial cultivation practice (operating since 2005)."},{"id":"computational-chemist","name":"Computational Chemist","domain":"chemistry","role":"DFT, molecular dynamics, computer-aided drug design.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are a Computational Chemist. You advise on DFT, molecular dynamics, docking, and ML for chemistry. You are precise about method limits and convergence, and you distinguish what is computed from what is assumed."},{"id":"extraction-formulation","name":"Extraction & Formulation Scientist","domain":"extraction","role":"CO2 extraction, purification, formulation.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are an Extraction & Formulation Scientist. You advise on supercritical CO2 and solvent extraction, purification, standardization, and stable formulation of fungal bioactives, with attention to yield, purity, and safety."},{"id":"mycology-research","name":"Mycology Research Specialist","domain":"mycology","role":"Fungal biology, bioactive compound discovery.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are a Mycology Research Specialist. You advise on fungal biology, taxonomy, secondary metabolites, and bioactive compound discovery. You cite mechanism and literature, and separate established findings from speculation."},{"id":"regulatory-affairs","name":"Regulatory Affairs Specialist","domain":"regulatory","role":"FDA, cGMP, supplement and food compliance.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are a Regulatory Affairs Specialist. You advise on FDA pathways, cGMP, DSHEA supplement rules, labeling, and claims substantiation for mushroom-derived products. You are conservative and cite the governing rule."},{"id":"facility-design","name":"Facility Design Engineer","domain":"engineering","role":"Grow rooms, extraction labs, cleanroom design.","authority":"advisory","autonomyCeiling":"plan","roomJoinable":true,"tools":[],"systemPrompt":"You are a Facility Design Engineer. You advise on grow-room, extraction-lab, and cleanroom design: airflow, HVAC, contamination control, throughput, and code compliance."},{"id":"scheduling","name":"Scheduling Agent","domain":"operations","role":"Calendar and Calendly scheduling.","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Scheduling agent. You manage calendar availability and Calendly bookings. You read freely; you confirm before creating, moving, or cancelling any event."},{"id":"sop","name":"SOP Agent","domain":"operations","role":"SOP and digital-product library management (R2 bucket swm-sops).","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe SOP agent. You manage the SOP and digital-product library (17 PDFs on Cloudflare R2, bucket swm-sops). You read and search freely; you confirm before publishing, replacing, or deleting any document."},{"id":"revenue","name":"Revenue Agent","domain":"operations","role":"Revenue and funnel briefing across Stripe and YouTube.","authority":"read_only","autonomyCeiling":"readonly","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Revenue agent. You produce honest revenue and funnel briefs from Stripe and YouTube data. You report verified numbers only and flag gaps. You are read-only."},{"id":"email","name":"Email Agent","domain":"operations","role":"Transactional and outreach email from michael@crowelogic.com.","authority":"read_confirm","autonomyCeiling":"edit","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Email agent. You draft and send transactional and outreach email from michael@crowelogic.com. You always show the full draft and recipient list and confirm before sending."},{"id":"auction","name":"Auction Agent","domain":"operations","role":"Auction valuation and max-bid discipline (read-only).","authority":"read_only","autonomyCeiling":"readonly","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Auction agent. You provide live valuation, net-margin ROI, and max-bid discipline for vehicles and haulables at online auctions. You compute fees (1.135x hammer + 175) and give clear win or walk calls. You are read-only and never place bids."},{"id":"studio","name":"Crowe Studio Director","domain":"media","role":"Grounded script, storyboard, and film direction.","authority":"read_only","autonomyCeiling":"readonly","roomJoinable":true,"tools":["terminal"],"systemPrompt":"You are the Crowe Studio Director. You write scripts, storyboards, and film direction grounded in the operator handbook and real footage, in the Crowe brand voice. No emojis, no em dashes, editorial tone. You can read assets and footage metadata."}]};

  var registry;
  function shimRequire(name) {
    if (name === "fs") return { readFileSync: function () { return JSON.stringify(VENDORED); } };
    if (name === "path") return { join: function () { return "agents.vendored.json"; } };
    if (name === "./registry") return registry;
    throw new Error("rooms-web: unbundled require(" + JSON.stringify(name) + ")");
  }

  var registryModule = { exports: {} };
  registry = (function (require, module, exports, __dirname) {
    // The room roster: which agents exist, and what each is allowed to be.
    //
    // Reads rooms/agents.vendored.json, the snapshot of the canonical registry in
    // michaelcrowe11/crowe-agents. Plain node, no Electron, no network, so the room
    // engine and its tests can load it anywhere.
    //
    // Room templates are named here rather than in the UI because a template is a
    // claim about which specialists belong in a conversation, and that is domain
    // knowledge, not layout. Each one is a real disagreement worth having: a
    // formulator against a regulator over a SKU, a grower against a facility
    // engineer over a room. A template of three interchangeable agents would be a
    // model bake-off wearing a product's clothes.

    const fs = require("fs");
    const path = require("path");

    const VENDORED = path.join(__dirname, "agents.vendored.json");

    // Ordered weakest to strongest. Rooms take the MINIMUM ceiling across their
    // roster, so this order is load-bearing rather than cosmetic.
    const TIERS = ["plan", "readonly", "edit", "execute"];
    const tierRank = (t) => { const i = TIERS.indexOf(String(t)); return i < 0 ? 0 : i; };

    let cache = null;
    function loadAgents() {
      if (cache) return cache;
      try {
        const d = JSON.parse(fs.readFileSync(VENDORED, "utf8"));
        cache = Array.isArray(d.agents) ? d.agents : [];
      } catch {
        // A missing snapshot means no rooms, not a crash on boot. The caller shows
        // an empty roster and the rest of the app is untouched.
        cache = [];
      }
      return cache;
    }

    function listAgents() { return loadAgents().filter((a) => a.roomJoinable !== false); }
    function getAgent(id) { return loadAgents().find((a) => a.id === String(id)) || null; }
    // roomJoinable is the mechanism for retiring an agent from rooms, so it has to
    // be asked at every point that seats one - composition, join, templates - not
    // only where the roster is listed. getAgent still returns a retired agent, so a
    // room saved before the retirement keeps showing who was in it.
    function isJoinable(id) { const a = getAgent(id); return Boolean(a) && a.roomJoinable !== false; }

    /* The ceiling a room may run at: the minimum across its roster, never the max.
       Stated as its own function because it is the one rule that must not be
       convenient. A room containing one advisory agent is an advisory room, even
       if the other two could write, because the alternative is that adding a
       cautious participant silently raises everyone else's authority. */
    function roomCeiling(agentIds) {
      const agents = (agentIds || []).map(getAgent).filter(Boolean);
      if (!agents.length) return "plan";
      return agents.reduce((low, a) => (tierRank(a.autonomyCeiling) < tierRank(low) ? a.autonomyCeiling : low), "execute");
    }

    /* The tier a room actually runs at: its own ceiling, further clamped by the
       app's configured autonomy. Both directions matter. The roster can never
       exceed what the operator set globally, and the operator's setting can never
       push an advisory agent into writing. */
    function effectiveTier(agentIds, configuredTier) {
      const ceiling = roomCeiling(agentIds);
      const cfg = TIERS.includes(String(configuredTier)) ? String(configuredTier) : "edit";
      return tierRank(cfg) < tierRank(ceiling) ? cfg : ceiling;
    }

    const writeCapable = (tier) => tierRank(tier) >= tierRank("edit");

    /* Templates are domain arguments, not agent counts.

       Each names agents by registry id. An id that no longer exists is dropped at
       compose time rather than throwing, so retiring an agent upstream degrades a
       template instead of breaking room creation. */
    const TEMPLATES = [
      {
        id: "product-review",
        name: "Product Review",
        purpose: "A formulation, argued against the rules it has to clear and the customers it has to reach.",
        agents: ["product-formulation", "regulatory-affairs", "commerce-support"],
        defaultAgent: "product-formulation",
      },
      {
        id: "grow-diagnosis",
        name: "Grow Diagnosis",
        purpose: "A room that is underperforming, read by the people who know the organism, the literature, and the building.",
        agents: ["cultivation-intelligence", "mycology-research", "facility-design"],
        defaultAgent: "cultivation-intelligence",
      },
      {
        id: "ship-it",
        name: "Ship It",
        purpose: "A change, checked by the operator who runs the estate and the auditor who has to evidence it.",
        agents: ["operator", "compliance-audit", "crowe-logic"],
        defaultAgent: "operator",
      },
      {
        id: "launch-review",
        name: "Launch Review",
        purpose: "A price, a funnel and an announcement, argued by the three people who each own one of them.",
        agents: ["revenue", "commerce-support", "email"],
        defaultAgent: "revenue",
      },
      {
        id: "security-posture",
        name: "Security Posture",
        purpose: "What the framework demands, against what the estate actually runs.",
        agents: ["compliance-audit", "operator", "crowe-logic"],
        defaultAgent: "compliance-audit",
      },
      {
        id: "molecule-triage",
        name: "Molecule Triage",
        purpose: "Worth pursuing, possible to model, and possible to actually extract. Three different answers.",
        agents: ["drug-discovery", "computational-chemist", "extraction-formulation"],
        defaultAgent: "drug-discovery",
      },
      {
        id: "the-week",
        name: "The Week",
        purpose: "What is booked, what is unanswered, and what actually earns. The three rarely agree.",
        agents: ["scheduling", "email", "revenue"],
        defaultAgent: "scheduling",
      },
      {
        id: "bake-off",
        name: "Bake-off",
        purpose: "One task, three deployments, no domain claim. Useful for comparing models and nothing else.",
        agents: ["crowe-logic", "crowelm-frontier", "operator"],
        defaultAgent: "crowe-logic",
      },
    ];

    // Agents come back resolved rather than as ids: every caller - the composer,
    // the room builder, the roster strip - wants the name and domain, and resolving
    // once here is what keeps a retired id from reaching any of them.
    function listTemplates() {
      return TEMPLATES.map((t) => ({ ...t, agents: t.agents.map(getAgent).filter((a) => a && a.roomJoinable !== false) }))
        .filter((t) => t.agents.length);
    }
    function getTemplate(id) { return listTemplates().find((t) => t.id === String(id)) || null; }

    module.exports = {
      listAgents, getAgent, isJoinable, listTemplates, getTemplate,
      roomCeiling, effectiveTier, writeCapable, tierRank, TIERS,
      _resetCache: () => { cache = null; },
    };

    return module.exports;
  })(shimRequire, registryModule, registryModule.exports, "");

  var engineModule = { exports: {} };
  var engine = (function (require, module, exports, __dirname) {
    // The room engine: several named agents and the operator in one thread.
    //
    // Everything here is pure orchestration over an injected runner. `runAgent` is
    // supplied by the caller - main.js hands it the real harness, scripts/test-rooms.js
    // hands it a fake - so addressing, visibility, concurrency, attribution, the
    // budget and the critique loop can all be exercised without a gateway. The
    // alternative is a feature whose only proof is a screenshot of it working once.
    //
    // The shape of the thing:
    //
    //   a room is a session         with a roster and a per-message author, stored
    //                               by the existing sessions store, not a new one
    //   a message names its author  "operator", or an agent id, never a generic
    //                               assistant label
    //   every agent sees everything the shared transcript IS the feature. An agent
    //                               that sees only its own turns is N sessions in
    //                               one window wearing a costume
    //   nobody speaks unaddressed   the single largest cost control here
    //
    // What this file deliberately does NOT do: write to the workspace. Gate 4 of
    // the build order is worktree isolation, and until that lands `roomTier()`
    // clamps every room to readonly. Three agents editing one tree is not a bug you
    // recover from by looping.

    const registry = require("./registry");

    const MAX_CRITIQUE_ROUNDS = 2;
    const DEFAULT_ROOM_BUDGET_USD = 1.0;

    /* The human's author id, and why it is not "operator".

       The registry contains an agent whose id is exactly `operator` - Crowe
       Operator, the one that runs the estate. Using the bare word for the person as
       well made the two indistinguishable in the transcript: an agent's answer was
       attributed to the human, flattened into the session as a user message, and
       would have been fed back to the next turn as though the operator had said it.

       Registry ids are lowercase kebab, so a leading colon cannot collide with one
       now or later. Everything that decides "is this the person" asks this constant
       rather than comparing to a string. */
    const HUMAN = ":operator";
    const isHuman = (author) => author === HUMAN;

    // ─── Addressing ──────────────────────────────────────────────────────────────

    /* Who a message is for.

       @room, or @<agent-id>, or nothing. Unaddressed goes to the room's default
       agent alone, never to everyone: a room where every stray message costs N
       calls is a room nobody can afford to think out loud in.

       Matching is on registry ids and on the agent's display name reduced to
       alphanumerics, because a handle cannot contain a space or an ampersand: what
       a person types for "Compliance & Audit" is "@ComplianceAudit", and the file
       calls it "compliance-audit". Both resolve. Unknown handles are reported
       rather than silently dropped, so a typo is visible instead of expensive. */
    function parseAddress(text, room) {
      const raw = String(text || "");
      const roster = room.agents.map((a) => a.agentId);
      const handles = new Map();
      for (const id of roster) {
        const agent = registry.getAgent(id);
        handles.set(id.toLowerCase(), id);
        if (agent) handles.set(String(agent.name).replace(/[^a-z0-9]/gi, "").toLowerCase(), id);
      }

      const mentions = raw.match(/@[A-Za-z0-9][\w.-]*/g) || [];
      if (mentions.some((m) => m.slice(1).toLowerCase() === "room")) {
        return { to: roster.slice(), broadcast: true, unknown: [] };
      }
      const to = [], unknown = [];
      for (const m of mentions) {
        const key = m.slice(1).toLowerCase();
        const id = handles.get(key);
        if (id) { if (!to.includes(id)) to.push(id); }
        else unknown.push(m);
      }
      if (to.length) return { to, broadcast: false, unknown };
      const fallback = room.defaultAgent && roster.includes(room.defaultAgent) ? room.defaultAgent : roster[0];
      return { to: fallback ? [fallback] : [], broadcast: false, unknown, defaulted: true };
    }

    // ─── The transcript one agent sees ───────────────────────────────────────────

    /* Every agent reads the whole room, including what the others produced and the
       tools they ran. That shared view is the entire point of the feature.

       Two framing rules, both about keeping an agent honest about its own place in
       the conversation:

         its own past turns come back as `assistant`, because they are its memory;
         every other agent's turn comes back as `user`, labelled with the speaker,
         because a model handed someone else's words as its own assistant history
         will continue them as if it had said them.

       The label is prose rather than a role field the gateway would have to
       understand, so this works on any OpenAI-compatible deployment. */
    function viewFor(room, agentId) {
      const out = [];
      for (const m of room.messages) {
        if (isHuman(m.author)) {
          out.push({ role: "user", content: m.content });
        } else if (m.author === agentId) {
          out.push({ role: "assistant", content: m.content });
        } else {
          const who = displayName(m.author);
          const kind = m.kind === "critique" ? `${who}, reviewing` : who;
          out.push({ role: "user", content: `[${kind}]\n${m.content}` });
        }
      }
      return out;
    }

    function displayName(agentId) {
      if (isHuman(agentId)) return "Operator";
      const a = registry.getAgent(agentId);
      return a ? a.name : String(agentId);
    }

    /* The standing instruction an agent carries in a room, on top of whatever its
       registry role says. Rooms are a social setting and the model has to be told
       the rules of it, or it invents them - which in practice means thanking its
       colleagues for their thorough review. */
    function roomBrief(room, agentId) {
      const me = registry.getAgent(agentId);
      const others = room.agents.filter((a) => a.agentId !== agentId).map((a) => displayName(a.agentId));
      return [
        me ? `You are ${me.name}. ${me.role}` : `You are ${agentId}.`,
        others.length
          ? `You are in a shared room with the operator and: ${others.join(", ")}. You can see their contributions and they can see yours.`
          : "You are in a room with the operator.",
        "Answer the operator and the room. Do not address the other participants directly, do not thank them, and do not comment on the quality of their work unless you are explicitly asked for a review.",
        "Speak from your own specialty. Where you disagree with what another participant concluded, say so plainly and give your reason.",
      ].join(" ");
    }

    // ─── Rooms ───────────────────────────────────────────────────────────────────

    function createRoom({ id, title, agentIds, defaultAgent, budgetUsd, template } = {}) {
      // Only joinable agents are seated. An id that exists but is retired from
      // rooms is dropped here rather than at display time, so no caller - the
      // composer, a template, or a raw IPC create - can compose around the flag.
      const ids = (agentIds || []).filter((x) => registry.isJoinable(x));
      return {
        id: id || "r-" + Date.now().toString(36) + "-" + Math.random().toString(36).slice(2, 7),
        kind: "room",
        title: title || "Untitled room",
        template: template || "",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        agents: ids.map((agentId) => ({ agentId, model: (registry.getAgent(agentId) || {}).model || "", state: "idle" })),
        defaultAgent: defaultAgent && ids.includes(defaultAgent) ? defaultAgent : ids[0] || "",
        messages: [],
        budgetUsd: typeof budgetUsd === "number" ? budgetUsd : DEFAULT_ROOM_BUDGET_USD,
        spentUsd: 0,
        cost: {},           // agentId -> { usd, promptTokens, completionTokens, calls }
        critiqueRounds: 0,
        halted: "",
      };
    }

    function fromTemplate(templateId, opts = {}) {
      const t = registry.getTemplate(templateId);
      if (!t) return null;
      return createRoom({
        ...opts,
        title: opts.title || t.name,
        template: t.id,
        agentIds: t.agents.map((a) => a.id),
        defaultAgent: t.defaultAgent,
      });
    }

    /* The tier a room may actually run at.

       Three clamps, and the order matters. The roster's minimum ceiling, then the
       app's configured autonomy, then the Gate 4 guard: until agents get isolated
       worktrees, no room writes anything, no matter who is in it or what the
       operator set. `allowWrites` is the switch that gate flips, and it defaults
       off so forgetting to pass it fails closed. */
    function roomTier(room, configuredTier, { allowWrites = false } = {}) {
      const ids = room.agents.map((a) => a.agentId);
      const tier = registry.effectiveTier(ids, configuredTier);
      if (!allowWrites && registry.writeCapable(tier)) return "readonly";
      return tier;
    }

    // ─── Cost ────────────────────────────────────────────────────────────────────

    function noteCost(room, agentId, { usd = 0, promptTokens = 0, completionTokens = 0 } = {}) {
      const c = room.cost[agentId] || (room.cost[agentId] = { usd: 0, promptTokens: 0, completionTokens: 0, calls: 0 });
      c.usd += usd || 0;
      c.promptTokens += promptTokens || 0;
      c.completionTokens += completionTokens || 0;
      c.calls += 1;
      // The room total is the sum of its parts by construction rather than by
      // coincidence: there is one place that adds, and it adds to both.
      room.spentUsd = Object.values(room.cost).reduce((s, x) => s + x.usd, 0);
      return c;
    }

    const overBudget = (room) => room.budgetUsd > 0 && room.spentUsd >= room.budgetUsd;

    /* What a round is about to cost, before it runs.

       Calls, not dollars, is the honest unit here: the price of a call depends on
       the deployment and the length of a transcript nobody has generated yet. A
       projected dollar figure would be a guess wearing a decimal point. The
       estimate is shown on the button, and the operator can cancel. */
    function projectRound(room, kind) {
      const live = room.agents.filter((a) => a.state !== "failed").length;
      if (kind === "critique") return { calls: live, agents: live, note: `${live} reviews, each agent over the others' work` };
      if (kind === "revise") return { calls: live, agents: live, note: `${live} revisions` };
      return { calls: live, agents: live, note: `${live} replies` };
    }

    // ─── Running a turn ──────────────────────────────────────────────────────────

    const setState = (room, agentId, state) => {
      const seat = room.agents.find((a) => a.agentId === agentId);
      if (seat) seat.state = state;
    };

    /* One addressed agent, one call.

       Failure is contained here rather than thrown: one agent failing marks that
       seat failed and leaves the room running. The status it lands on is the one
       thing this function must never lie about - a seat that errored says failed,
       and its output does not enter the transcript, so it cannot be critiqued as
       if it were work. */
    async function runOne(room, agentId, deps, { kind = "reply", brief = "" } = {}) {
      setState(room, agentId, "working");
      const seat = room.agents.find((a) => a.agentId === agentId) || {};
      const messages = viewFor(room, agentId);
      if (brief) messages.push({ role: "user", content: brief });

      let res;
      try {
        res = await deps.runAgent({
          agentId,
          roomId: room.id,
          model: seat.model || "",
          systemBrief: roomBrief(room, agentId),
          messages,
          tier: room.tier || "readonly",
        });
      } catch (e) {
        setState(room, agentId, "failed");
        return { agentId, ok: false, error: String((e && e.message) || e) };
      }

      if (res && res.usage) noteCost(room, agentId, res.usage);

      if (!res || res.error || res.stopped) {
        setState(room, agentId, res && res.stopped ? "stopped" : "failed");
        return { agentId, ok: false, error: (res && (res.error || "stopped")) || "no result", stopped: !!(res && res.stopped) };
      }

      const text = String(res.text || "").trim();
      if (!text) { setState(room, agentId, "failed"); return { agentId, ok: false, error: "empty answer" }; }

      const msg = { author: agentId, content: text, kind, at: Date.now(), replyTo: kind === "critique" ? res.replyTo || "" : "" };
      room.messages.push(msg);
      room.updatedAt = Date.now();
      setState(room, agentId, "done");
      return { agentId, ok: true, text, message: msg };
    }

    /* The operator says something; whoever is addressed answers.

       Concurrently, not round-robin: three specialists asked the same question
       should be able to think at once, and a room that serialises them for no
       reason feels broken in a way no amount of status text repairs. */
    async function speak(room, text, deps, { author = HUMAN } = {}) {
      if (room.halted) return { halted: room.halted, ran: [] };
      const addr = parseAddress(text, room);
      room.messages.push({ author, content: String(text), kind: "say", at: Date.now(), to: addr.to.slice() });
      room.updatedAt = Date.now();

      // Everyone not addressed is explicitly idle rather than left on whatever they
      // were, so "queued" never lingers on an agent that is not going to run.
      for (const seat of room.agents) seat.state = addr.to.includes(seat.agentId) ? "queued" : "idle";

      if (overBudget(room)) { room.halted = "budget"; return { halted: "budget", ran: [], address: addr }; }

      const results = await Promise.all(addr.to.map((id) => runOne(room, id, deps, { kind: "reply" })));
      if (overBudget(room)) room.halted = "budget";
      return { ran: results, address: addr, halted: room.halted };
    }

    // ─── Critique and revise ─────────────────────────────────────────────────────

    const lastRoundBy = (room, kind) => {
      const out = new Map();
      for (const m of room.messages) if (m.kind === kind) out.set(m.author, m);
      return out;
    };

    /* The move worth stealing, promoted from a prompt to a primitive.

       Each participating agent reviews the OTHERS' last outputs and is told not to
       review its own. That instruction is the whole mechanism: the round where
       agents looked at each other's work is where quality came from, and an agent
       grading its own paper is where it does not.

       A failed agent's output is excluded - both from what gets reviewed and from
       who reviews - because critiquing an error message teaches nobody anything. */
    async function critique(room, deps) {
      if (room.halted) return { halted: room.halted, ran: [] };
      if (room.critiqueRounds >= (room.maxCritiqueRounds || MAX_CRITIQUE_ROUNDS)) {
        return { capped: true, ran: [], rounds: room.critiqueRounds, max: room.maxCritiqueRounds || MAX_CRITIQUE_ROUNDS };
      }
      if (overBudget(room)) { room.halted = "budget"; return { halted: "budget", ran: [] }; }

      const round = lastRoundBy(room, "reply");
      const authors = [...round.keys()].filter((id) => (room.agents.find((a) => a.agentId === id) || {}).state !== "failed");
      if (authors.length < 2) return { ran: [], note: "critique needs at least two standing positions" };

      room.critiqueRounds += 1;
      for (const seat of room.agents) seat.state = authors.includes(seat.agentId) ? "queued" : "idle";

      const results = await Promise.all(authors.map((id) => {
        const others = authors.filter((o) => o !== id)
          .map((o) => `--- ${displayName(o)} ---\n${round.get(o).content}`).join("\n\n");
        const brief = [
          `Review the following contributions from the other participants. Do not review your own; it is not included.`,
          ``, others, ``,
          `For each one: name what is wrong, what is missing, and what would have to be true for it to hold. Be specific and cite the claim you are contesting. Where it is right, say so briefly and move on. Do not summarise, do not compliment, and do not restate your own position.`,
        ].join("\n");
        return runOne(room, id, deps, { kind: "critique", brief });
      }));

      if (overBudget(room)) room.halted = "budget";
      return { ran: results, round: room.critiqueRounds, halted: room.halted };
    }

    /* The other half of the loop. Each agent is handed the critiques written about
       it and asked to act on them, including the option of holding its position
       with a reason - a revision that caves to every note is as useless as one that
       ignores them. */
    async function revise(room, deps) {
      if (room.halted) return { halted: room.halted, ran: [] };
      if (overBudget(room)) { room.halted = "budget"; return { halted: "budget", ran: [] }; }

      const critiques = room.messages.filter((m) => m.kind === "critique");
      if (!critiques.length) return { ran: [], note: "nothing to revise: run a critique round first" };

      const positions = lastRoundBy(room, "reply");
      const targets = [...positions.keys()].filter((id) => (room.agents.find((a) => a.agentId === id) || {}).state !== "failed");
      for (const seat of room.agents) seat.state = targets.includes(seat.agentId) ? "queued" : "idle";

      const results = await Promise.all(targets.map((id) => {
        const about = critiques.filter((c) => c.author !== id)
          .map((c) => `--- ${displayName(c.author)} ---\n${c.content}`).join("\n\n");
        const brief = [
          `The other participants reviewed your position. Their reviews:`, ``, about, ``,
          `Give your position again, revised. Where a review changed your mind, change it and say which point did. Where you are holding your position, say that plainly and give the reason the review did not move you. Do not thank anyone.`,
        ].join("\n");
        return runOne(room, id, deps, { kind: "reply", brief });
      }));

      if (overBudget(room)) room.halted = "budget";
      return { ran: results, halted: room.halted };
    }

    // ─── Persistence shape ───────────────────────────────────────────────────────

    /* A room is a session. It rides the existing sessions store rather than a
       second database, so it inherits its listing, deletion and backup for free.
       `kind: "room"` is what tells them apart, and its absence is what makes every
       session written before rooms existed still load as an ordinary thread. */
    function toSession(room) {
      return {
        id: room.id, kind: "room", title: room.title, updatedAt: room.updatedAt,
        room: {
          template: room.template, agents: room.agents, defaultAgent: room.defaultAgent,
          budgetUsd: room.budgetUsd, spentUsd: room.spentUsd, cost: room.cost,
          critiqueRounds: room.critiqueRounds, halted: room.halted,
        },
        messages: room.messages,
      };
    }

    function fromSession(d) {
      if (!d || d.kind !== "room" || !d.room) return null;
      return {
        id: d.id, kind: "room", title: d.title || "Untitled room",
        template: d.room.template || "", createdAt: d.createdAt || d.updatedAt || Date.now(),
        updatedAt: d.updatedAt || Date.now(),
        agents: (d.room.agents || []).map((a) => ({ ...a, state: "idle" })),
        defaultAgent: d.room.defaultAgent || "",
        messages: Array.isArray(d.messages) ? d.messages : [],
        budgetUsd: typeof d.room.budgetUsd === "number" ? d.room.budgetUsd : DEFAULT_ROOM_BUDGET_USD,
        spentUsd: d.room.spentUsd || 0,
        cost: d.room.cost || {},
        critiqueRounds: d.room.critiqueRounds || 0,
        halted: d.room.halted || "",
      };
    }

    /* The transcript as an ordinary session would have written it.

       This is what makes the one-agent parity claim checkable rather than asserted:
       a room with a single agent, flattened, must be exactly the message list the
       operator thread would have persisted for the same conversation. */
    function toPlainMessages(room) {
      return room.messages
        .filter((m) => m.kind !== "critique")
        .map((m) => ({ role: isHuman(m.author) ? "user" : "assistant", content: m.content }));
    }

    module.exports = {
      HUMAN, isHuman,
      createRoom, fromTemplate, parseAddress, viewFor, roomBrief, displayName,
      speak, critique, revise, runOne,
      noteCost, overBudget, projectRound, roomTier,
      toSession, fromSession, toPlainMessages,
      MAX_CRITIQUE_ROUNDS, DEFAULT_ROOM_BUDGET_USD,
    };

    return module.exports;
  })(shimRequire, engineModule, engineModule.exports, "");

  window.CroweRooms = Object.freeze({
    engine: engine,
    registry: registry,
    source: Object.freeze({"engine":"cfbc36df7cc5","registry":"9945cdb73edc","vendored":"68bc68da93b8"}),
  });
})();
