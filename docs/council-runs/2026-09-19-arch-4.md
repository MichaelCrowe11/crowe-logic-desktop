# Council receipt arch-4

Status: **escalated**. Proposal needs a kind and a bounded summary.

Goal: Continue the council's best proposed architecture implementation for the next version of Crowe Logic's council autopilot.

Steps 1 and 2 are already proposed and council-approved under previous grants. Their full text, votes and receipts are in the supplied files docs/council-runs/2026-09-19-arch-1.md (Step 1: one mandatory host-side authorization boundary for every Room action) and docs/council-runs/2026-09-19-arch-2.md (Step 2: cross-device continuity as a replicated view of one authoritative Room journal, never replication of executable authority). Do not re-propose them. Build on them.

A first attempt at Step 3 was escalated by the safety classifier before any vote; its text and the classifier's objections are in docs/council-runs/2026-09-19-arch-3.md. The objections were: reservations based on estimated prompt tokens are not a guaranteed bound; maxOutputTokens was not enforced on the outgoing request; settled spend was allowed to exceed the estimate; enforcement against both the grant limit and the Room budget was unspecified; future council-cycle prompts had no demonstrated upper cost bound. The new Step 3 must answer each objection explicitly, or state plainly which guarantee is not achievable and why.

Hard format bound: the protocol rejects any proposal whose summary exceeds 8000 characters, and the rejection ends the grant. Keep every summary under 7000 characters.

The supplied read-only evidence is the current implementation: rooms/council.js (the shared protocol), rooms/council-host.js (the desktop host behind IPC), rooms/council-files.js (the scoped file executor), rooms/registry.js (catalog models as Room contacts, tiers and ceilings), rooms/engine.js (the Room runner: seats, turns, critique rounds, budgets and spend), companion.js (the phone companion: pairing, remote commands, audit) and docs/COUNCIL-AUTOPILOT.md (the operating contract and its stated limits).

Each proposal is one bounded architecture step: a single coherent design decision with its rationale, the interfaces or data it introduces or changes, the safety invariants it preserves or strengthens, how it would be verified, and its known limits. Ground every claim in the supplied source; do not invent behavior the files do not show.

Remaining plan items, in order:
3. Strict spend accounting: a dollar ceiling that holds under parallel rounds, and a web runner that records real cost instead of zero.
4. Mitigation of correlated reviewer failure: shared blind spots, prompt injection through evidence, and a proposer that games its reviewers. Include three weaknesses observed in the first real runs: an advisory receipt without recorded content gives the verifier nothing to check; the proposer prompt does not state the 8000-character summary bound, so an over-long proposal ends the grant; and the safety classifier can end a grant on design disagreement without a vote, because its prompt does not separate safety findings from design critique.
5. Oversight metrics the product can measure and show: action coverage, intervention latency, false approvals and false refusals.

This grant is advisory. Do not propose file changes. After items 3, 4 and 5 are covered, the final proposal must be of kind "complete" and its summary must be the ordered plan for Steps 1 through 5: one short paragraph per step, in build order, naming the files a maintainer would touch first.

Mode advisory; quorum 3; steps 0/6; calls 1/50.
Seats: crowelm-zenith (GPT-6 Astra), crowelm-kernel (Claude Fable 5.1), crowelm-depth (Grok 4.6), deepseek-v4-pro (DeepSeek V4 Pro), crowelm-coder (GPT-5.6 Sol).
Writable scope: none. Read-only context: rooms/council.js, rooms/council-host.js, rooms/council-files.js, rooms/registry.js, rooms/engine.js, companion.js, docs/COUNCIL-AUTOPILOT.md, docs/council-runs/2026-09-19-arch-1.md, docs/council-runs/2026-09-19-arch-2.md, docs/council-runs/2026-09-19-arch-3.md.
