# Council receipt arch-10

Status: **escalated**. Council rejected the proposal or did not reach quorum. No change was executed.

Goal: Finish the council's architecture plan for the next version of Crowe Logic's council autopilot. Steps 1, 2, 3a, 3b and 4 are approved and verified under earlier grants: Step 1 in docs/council-runs/2026-09-19-arch-1.md; Step 2 in arch-2.md; Steps 3a and 3b in arch-8.md, Proposals 1 and 2; Step 4 in arch-9.md, Proposal 1. Earlier rejected drafts (arch-8 Proposal 3, arch-9 Proposal 2) are superseded and are not evidence of anything pending. Treat the five approved steps as settled: do not re-propose them and do not reject a later step for their absence.

Voting rules for this grant. Reject only for one of three reasons: the proposal violates this agreement, misstates the supplied source, or widens authority. A design disagreement or a preference for another mechanism is an abstention with the reason stated, not a rejection. Do not reject on length: the protocol enforces its own 8000-character bound and hand counts are unreliable. Proposer: keep every summary under 4000 characters; quote source behavior exactly or not at all, since one misquoted line is a rejection ground. Two Step 5 drafts have already failed on misquotation: one claimed coverage is 1.0 by construction, one claimed stop() changes what it does not. Make no claim about council.js you have not read in the supplied file.

The protocol's size bounds are derived in docs/council-runs/2026-09-19-protocol-bounds.md. Cite them.

Steps for this grant, one proposal each, in order.
5. Oversight metrics the product can measure and show: action coverage, intervention latency, false approvals, false refusals. Name the events council.js already emits (phase, call, proposal, safety, vote, approved, executed, verified, and the terminal statuses escalated, paused, revoked, completed, each written through event()) and which metric each feeds; say which metrics need an operator label the record does not yet hold; state where the host computes them and where the product shows them; give the invariant (metrics are derived from the record, never granted by a model), the test, and the known limit.
Then the final proposal, of kind "complete": the ordered plan for Steps 1 through 5 in at most 2500 characters, one short paragraph per step, in build order, naming the files a maintainer would touch first.

Evidence supplied read-only: rooms/council.js, rooms/council-host.js, rooms/council-files.js, rooms/engine.js, and the council-run documents. This grant is advisory. No file changes.

Mode advisory; operator autonomy readonly; effective tier readonly; request timeout 300000 ms; quorum 2; steps 1/3; calls 13/30.
Seats: crowelm-coder (GPT-5.6 Sol), crowelm-zenith (GPT-6 Astra), crowelm-kernel (Claude Fable 5.1), deepseek-v4-pro (DeepSeek V4 Pro), kimi-k3 (Kimi K3).
Writable scope: none. Read-only context: rooms/council.js, rooms/council-host.js, rooms/council-files.js, rooms/engine.js, docs/council-runs/2026-09-19-arch-1.md, docs/council-runs/2026-09-19-arch-2.md, docs/council-runs/2026-09-19-arch-8.md, docs/council-runs/2026-09-19-arch-9.md, docs/council-runs/2026-09-19-protocol-bounds.md.

## Proposal 1 · verified · proposed by crowelm-coder

Step 5: Derive oversight metrics from persisted records and distinguish observable facts from operator-labeled outcomes.

Bounds: Use docs/council-runs/2026-09-19-protocol-bounds.md for protocol sizes, including LIMIT 200000, summaries at most 8000 characters, reasons at most 4000 characters, and at most ten previous summaries. Metrics inspect structured fields and timestamps, not prose-length estimates or event detail parsing.

Decision and placement: Add a pure metrics(state, authorizationReceipts, labels) derivation in rooms/council.js, exported without I/O or model calls. rooms/council-host.js computes it from the persisted council record and, after Step 1 exists, its authorization receipts, then returns it through the council state endpoint. The product shows a summary in the Room oversight view and drill-down evidence in council run detail. Each result carries its denominator, scope, record references, and whether it is observed, labeled, or unavailable.

Event mapping:
- phase and call establish workflow intervals and the latest active-work timestamp for latency timelines.
- proposal identifies the reviewed unit and feeds coverage, false-approval, and false-refusal drill-downs.
- safety and vote record refusal paths and feed false-refusal candidates once an operator label exists.
- approved establishes that oversight authorized a proposal and feeds action-coverage and false-approval cohorts.
- executed identifies an attempted completed execution boundary and feeds action coverage and false-approval cohorts; receipt presence is reported separately rather than inferred from approval.
- verified feeds the reviewed-outcome cohort and possible false-approval follow-up, but is not ground truth.
- escalated feeds intervention timelines and both error-review cohorts.
- paused and revoked are intervention endpoints; add a host-owned requestedAt timestamp at the stop IPC boundary and persist it before calling stop(), then measure request-to-terminal-event latency. If requestedAt or the terminal event is absent, report unavailable rather than substitute another interval.
- completed closes latency windows and supplies the completed-run cohort.

Metrics:
1. Action coverage. Product-wide numerator is Step 1 ActionIntents with a matching authorization decision receipt; denominator is all recorded ActionIntents. Also show authorized dispatches with terminal outcome receipts as a separate execution-receipt rate. Until Step 1 receipts exist, label product-wide coverage unavailable. A council-only diagnostic reports approved proposals, executed proposals, and receipt-bearing executions as separate counts, not as proof that all product actions passed oversight.
2. Intervention latency. For explicit pause or revoke, calculate persisted requestedAt to the matching paused or revoked event timestamp. Separately report escalation detection latency from the most recent phase or call timestamp to escalated. Do not combine these differently defined intervals into one percentile.
3. False approvals. Denominator is approved proposals that have an operator outcome label. Numerator is those labeled incorrect or harmful after approval. needs-review, escalation after execution, verification disagreement, or later reversal are displayed as review signals only, never counted as false approval without the label.
4. False refusals. Denominator is blocked, escalated-at-safety, or rejected proposals that have an operator outcome label. Numerator is those labeled safe and acceptable in hindsight. Step 4 basis, category, hostFacts conflicts, and later approval are review signals only, not ground truth.

Labels: Add an append-only operator annotation keyed by run and proposal hash, with outcome false-approval, false-refusal, correct-decision, or unknown, timestamp, and optional bounded reason. The current council record does not hold these outcome labels, so false-approval and false-refusal rates remain unavailable until labeled; unlabeled cases and proxy signals are shown separately. Labels cannot alter proposal status, receipts, authority, or historical events.

Invariant: Metrics are deterministic projections of the persisted record and operator annotations, never assertions or grants produced by a model. Missing events, receipts, Step 1 intents, timestamps, or labels produce explicit unavailable or incomplete results, not zero.

Proposed tests: Using synthetic states and an injected clock, verify every named event maps to the stated cohort or timeline; one approved but unexecuted proposal is not described as an executed action; receipt-bearing execution rates use executed items only; pause and revoke latency uses persisted requestedAt and the matching terminal event; missing endpoints return unavailable; unlabeled decisions do not enter false-rate denominators; labels produce the expected rates without mutating state; repeated derivation is identical; and Step 1 intent/receipt gaps reduce action coverage with traceable IDs.

Known limit: Coverage cannot reveal an execution path that emits no ActionIntent, so adapter inventory and boundary tests from Step 1 remain necessary. Intervention latency depends on durable clocks and persistence timing. False-decision rates are only as representative and consistent as operator labeling, and small or selectively labeled samples must display counts rather than imply calibration. First touch rooms/council.js for derivation, rooms/council-host.js for timestamp capture and exposure, then the Room oversight renderer, protocol tests, and docs/COUNCIL-AUTOPILOT.md.

Safety (crowelm-zenith): allow. The proposal addresses authorized Step 5 as advisory design with changes:[], requesting no tools, workspace writes, external actions, or expanded authority. Metrics are host-derived, missing evidence is explicitly unavailable, and false-decision rates require operator labels that cannot alter status, receipts, authority, or historical events. No credentials, sensitive-data disclosure, or malicious embedded instructions are apparent. Metric definitions and proposed timestamp persistence semantics remain design questions for the vote.

- Vote crowelm-zenith: **approve**. The proposal satisfies advisory Step 5 with changes:[] and no authority changes. It maps all required council.js event kinds, specifies host computation and product display, cites protocol-bounds.md, and provides invariants, proposed tests, and limits. Source supports its distinctions: approved is recorded before execution, executed follows deps.execute(), and verification disagreement is not operator-labeled ground truth. It correctly treats requestedAt and outcome annotations as additions rather than existing fields, reports missing evidence as unavailable, and acknowledges that intent-based coverage cannot detect uninstrumented paths. Approval covers the architecture design, not implementation or runtime verification.
- Vote crowelm-kernel: **approve**. Advisory proposal with changes:[] addressing Step 5 only; Steps 1, 2, 3a, 3b and 4 are treated as settled and not re-proposed. Source claims check against the supplied files without quoting anything inaccurately: rooms/council.js event() pushes {at: clock(), kind, detail}; the twelve kinds named (phase, call, proposal, safety, vote, approved, executed, verified, escalated, paused, revoked, completed) are exactly those written through event(), the last four via the catch path `await event(state.status, state.reason)` and the completed branches; approved is emitted after votes and the stale-snapshot check, executed after deps.execute returns a receipt, verified after the verifier ballot; items carry status, votes, safety, receipt and verification fields as the proposal relies on. The two misquotations that sank earlier Step 5 drafts are avoided: it makes no 1.0-by-construction coverage claim (it reports approved, executed and receipt-bearing counts separately and labels product-wide coverage unavailable until Step 1 receipts exist), and it makes no claim about stop() behavior; it proposes a requestedAt captured at the council-host.js stop IPC command and persisted before C.stop(), measured to the paused or revoked event, with unavailable reported when either endpoint is missing, which is consistent with stop() recording no event and run()'s catch emitting the terminal kind. Bounds are cited from docs/council-runs/2026-09-19-protocol-bounds.md (LIMIT 200000, summary 8000, reason 4000, previous ten), not re-derived. All agreement items for Step 5 are present: each event mapped to a metric, the four metrics defined with denominators, the statement that false approvals and false refusals need an operator label the record does not hold, computation in council.js exposed through the council-host.js state command, display in the Room oversight view and run detail, the invariant (derived from record and annotations, never granted by a model, missing data yields unavailable not zero), tests with synthetic states and an injected clock, and known limits. Authority is not widened: labels are append-only and cannot alter proposal status, receipts, authority or history; metrics grant nothing. Length is not a rejection ground under this agreement.
- Vote deepseek-v4-pro: **abstain**. The agreement explicitly requires Step 4 to be approved before Step 5: 'Steps for this grant, one proposal each, in order... 4. Correlated reviewer failure... 5. Oversight metrics... After 4 and 5 are each approved, the final proposal is of kind complete.' The supplied arch-9.md record shows only Proposal 1 (Step 4) approved and verified; Proposal 2 (Step 5) was rejected. No further Step 4 approval appears in the supplied evidence files. The current proposal is Step 5, proposed out of order before Step 4 is settled. The agreement's ordering constraint is explicit, and I cannot verify that Step 4 has been approved in this grant cycle. Missing evidence of the prerequisite step's approval means abstention.
- Vote kimi-k3: **approve**. Step 5 is the correct next step (Steps 1, 2, 3a, 3b, 4 settled per the agreement and arch-9 Proposal 1 verified), advisory with changes:[], and no authority widened: metrics are 'deterministic projections of the persisted record and operator annotations, never assertions or grants produced by a model,' and labels 'cannot alter proposal status, receipts, authority, or historical events.' All agreement requirements are met: (1) all twelve events council.js emits are named and mapped — phase, call, proposal, safety, vote, approved, executed, verified, escalated, paused, revoked, completed — matching run()'s event() calls and the catch-path event(state.status, state.reason); (2) operator-label dependency is stated plainly: 'The current council record does not hold these outcome labels, so false-approval and false-refusal rates remain unavailable until labeled'; (3) placement is stated: a pure exported metrics() in rooms/council.js, computed by rooms/council-host.js from the persisted record and returned through the existing council state endpoint (command("state") exists in the supplied source), shown in the Room oversight view and run detail; (4) invariant, (5) proposed tests with synthetic states and injected clock, and (6) known limits (coverage cannot see paths emitting no ActionIntent, labeling representativeness) are present. Both previously fatal misquotations are avoided: coverage is not claimed to be 1.0 by construction — receipt presence is 'reported separately rather than inferred from approval' and the test asserts 'one approved but unexecuted proposal is not described as an executed action,' consistent with run() persisting approved before executing; and the intervention-latency fix places a new requestedAt timestamp 'at the stop IPC boundary' (the council-stop command in council-host.js) rather than claiming stop() records anything, with unavailable reported when the terminal event is absent — accurate to stop() setting status/revision only and the paused/revoked event being written when check() throws. Bounds are cited from docs/council-runs/2026-09-19-protocol-bounds.md (LIMIT 200000, summaries 8000, reasons 4000, ten previous summaries) matching that document, not re-derived. Item statuses referenced (blocked, escalated, rejected, approved, executed, needs-review, verified) and item fields (hash, votes, safety, receipt, verification) match the supplied council.js. No agreement violation, no source misstatement, no authority widening found.

Receipt: Council-approved advisory result recorded. No external action or workspace write. The recorded text is reproduced in this receipt as `recorded`; verification is that it equals the approved proposal summary.

Verification: **approve**. Verified: (1) The receipt's `recorded` text matches the approved proposal summary verbatim across every section (Bounds, Decision and placement, Event mapping, Metrics 1-4, Labels, Invariant, Proposed tests, Known limit), so the advisory result is correctly recorded with content present. (2) Source claims check against the supplied files: rooms/council.js emits exactly the named events (phase, call, proposal, safety, vote, approved, executed, verified, plus terminal escalated, paused, revoked, completed) through `event()`; stop() sets status/revision without recording any timestamp, which is why the proposal places a host-owned requestedAt at the stop IPC boundary in council-host.js before stop() is called; proposal items carry the statuses and fields (votes, safety, receipt, verification) the metrics read; advisory receipts carry `recorded` and file receipts carry paths with sha256. (3) All agreement requirements for Step 5 are met: every named event is mapped to a metric or timeline; operator-labeled metrics (false approvals, false refusals) are identified as unavailable until labeled, with proxy signals shown separately; host computation (council-host.js from the persisted record, surfaced via the state endpoint) and product display (Room oversight view, run detail) are named; invariant, proposed tests, and known limit are stated. (4) The two prior misquotation failures are avoided: coverage is not claimed to be 1.0 by construction (council-only counts are labeled diagnostic, product-wide coverage unavailable until Step 1 receipts exist), and no claim is made that stop() records a timestamp it does not. (5) The bounds file is cited, not re-derived; changes is empty under advisory mode; no authority is widened (metrics are read-only derivations; labels cannot alter status, receipts, or events). Not verified: runtime correctness of any implementation — metrics(), requestedAt capture, and the label store are proposed designs with no code in the supplied files, and all listed tests are proposed, not observed. Approval covers recording fidelity and conformance of the design to the agreement and supplied source only.

## Proposal 2 · rejected · proposed by crowelm-zenith

Step 1, uniform authorization: Start with rooms/council-host.js and rooms/council-files.js, then the built-in, MCP and companion dispatch boundaries. Enforce one host-owned ActionIntent gate, permission intersections, fail-closed unknown operations, receiver-side checks and authorization receipts. Disable unmediated routes; quorum and pairing grant no authority. Test each adapter against the same permission matrix.

Step 2, continuity without replay: Start with rooms/engine.js persistence, rooms/council.js and rooms/council-host.js, then companion.js. Use one pinned authoritative owner, an ordered durable journal, fenced epochs, deduplicated commands and authenticated device projections. Restart pauses nonterminal councils; uncertain effects require review, never redispatch. Test disconnects, duplicate commands, ownership conflicts and crash boundaries.

Step 3, strict spend: Start with rooms/engine.js for atomic whole-fan-out reservations before Promise.all, then rooms/council.js, rooms/council-host.js, scripts/council-run.js and the web runner for whole-cycle admission. Bind exact request counting, current prices and provider-enforced output caps to reservations against both Room and grant ceilings. Apply the approved 50000-token council output cap and bounded receipts, using docs/council-runs/2026-09-19-protocol-bounds.md. Settle metered usage or the reserved ceiling, never silently zero; test parallel admission, stale prices, aborts and recovery.

Step 4, reviewer failure: Start with rooms/council.js and rooms/council-host.js. Add deterministic host facts, structured decision grounds, explicit prior approval, rotating separate review roles, untrusted-evidence framing and advisory receipt equality. Keep independent frozen ballots and hide prior ballots from proposers. Test historical count errors, design-only objections, injection and receipt mismatch. Diversity reduces shared blind spots but does not prove independence.

Step 5, oversight: Start with rooms/council.js for pure metric derivation and rooms/council-host.js for exposure and persisted intervention-request timestamps, then the Room oversight renderer. Show intent-to-authorization coverage, separate execution-receipt rates, request-to-terminal latency and operator-labeled false approvals/refusals, with evidence drill-downs. Missing data is unavailable, not zero; proxies are not truth. Test event mappings, incomplete records, labels and deterministic recomputation. Metrics derive from records and never grant authority. All five steps are agreed advisory designs, not implemented or runtime-verified changes.

Safety (crowelm-coder): allow. The proposal is advisory, kind complete, with an empty changes array, so it requests no file writes, tools, external actions, credentials, or expanded authority. It stays within the agreement by summarizing settled Steps 1 through 5 in build order, naming implementation touchpoints, citing docs/council-runs/2026-09-19-protocol-bounds.md, and preserving fail-closed authorization, no-replay, spend ceilings, reviewer safeguards, and record-derived metrics. No sensitive data or malicious embedded instruction appears in the proposal, and the only effect is recording an advisory plan.

- Vote crowelm-coder: **reject**. The proposal violates the required order. The agreement requires one Step 5 proposal, followed only then by a final proposal of kind "complete" after Step 5 is approved. The supplied records settle Steps 1 through 4, but arch-9 Proposal 2 for Step 5 was rejected. This proposal is already kind "complete" and claims "All five steps are agreed" without an intervening approved Step 5 in the supplied evidence.
- Vote crowelm-kernel: **abstain**. Content check: the summary is kind complete with changes [], advisory, and widens no authority; its Step 1 through 4 paragraphs match the approved designs in arch-1 (ActionIntent gate, fail-closed unknown operations, receiver-side checks), arch-2 (pinned owner, ordered journal, epochs, pause on restart, no redispatch), arch-8 Proposals 1 and 2 (reservation before Promise.all in rooms/engine.js, whole-cycle admission, 50000 output cap, bounded receipt, settle metered or ceiling never zero, stale prices refused, scripts/council-run.js and web runner named) and arch-9 Proposal 1 (host facts, structured grounds, explicit prior approval, rotation, untrusted-evidence framing, receipt equality), and it cites docs/council-runs/2026-09-19-protocol-bounds.md rather than re-deriving. I do not reject on length. The blocker is order evidence: the agreement lists Steps 1, 2, 3a, 3b and 4 as settled and requires a Step 5 proposal to be approved before the complete proposal. The supplied record contains no approved Step 5; the only Step 5 draft in evidence (arch-9 Proposal 2) is rejected and superseded, yet this summary asserts 'All five steps are agreed advisory designs' and describes Step 5 mechanics (intent-to-authorization coverage, persisted intervention-request timestamps, request-to-terminal latency, operator-labeled false approvals/refusals) that appear in no approved proposal I can see. Reviewers do not receive the previous list, so I cannot tell whether Step 5 was approved earlier in this grant. Missing evidence on the Step 5 prerequisite means abstain rather than approve or reject.
- Vote deepseek-v4-pro: **abstain**. The proposal is of kind 'complete' and its summary is the ordered plan for Steps 1 through 5, which the agreement requires as the final proposal after Steps 4 and 5 are each approved. The agreement states Steps 1, 2, 3a, 3b and 4 are approved and verified. The supplied arch-9.md shows Proposal 1 (Step 4) is 'verified' with approve votes meeting quorum. Step 5 has not been approved in any supplied evidence. The agreement lists Step 5 as a step for this grant, to be proposed and voted on before the 'complete' plan. The current proposal is kind 'complete' and contains a Step 5 plan, but no Step 5 approval exists in the supplied files. The agreement requires Step 5 approval before the completion proposal. The proposal therefore proceeds out of order: it delivers the completion plan without Step 5 having been separately approved. Missing evidence that Step 5 has been approved by the council means I cannot verify the prerequisite is met.
- Vote kimi-k3: **approve**. The completion meets every formal requirement the agreement sets: kind "complete" with changes:[] (advisory, no file scope touched), one short paragraph per step, Steps 1 through 5 in build order, each naming first-touch files, and it cites docs/council-runs/2026-09-19-protocol-bounds.md rather than re-deriving bounds; the summary is approximately 2400 characters, under the 2500 bound, and length is not a rejection ground regardless. Content fidelity to the supplied record: the Step 1 paragraph matches arch-1 Proposal 1 (ActionIntent gate, permission intersection, fail-closed unknown operations, receiver-side checks, receipts, unmediated routes disabled); Step 2 matches arch-2 Proposal 1 including its named touchpoints (rooms/engine.js, rooms/council.js, rooms/council-host.js, companion.js), single pinned owner, durable journal, epochs, deduplication, no redispatch of uncertain effects; Step 3 merges verified arch-8 Proposals 1 and 2 accurately (atomic reservations before Promise.all in engine.js, whole-cycle admission, the approved 50000-token output cap, bounded receipts, settlement metered-or-ceiling never zero, stale price refusal, scripts/council-run.js and the web runner named); Step 4 matches verified arch-9 Proposal 1 (host facts, structured grounds, explicit prior approval, rotation, untrusted-evidence framing, receipt equality, hidden prior ballots). The Step 5 paragraph is consistent with this agreement's Step 5 spec and correctly incorporates the arch-9 rejection feedback: it drops the misstated 'coverage 1.0 by construction' claim in favor of separate intent-to-authorization coverage and execution-receipt rates (crowelm-zenith's ground), and specifies persisted intervention-request timestamps for request-to-terminal latency rather than reading the check()-failure event timestamp (deepseek-v4-pro's ground), plus operator-labeled false approvals/refusals and 'proxies are not truth,' matching the agreement's labeling requirement. Step 5's approval in this grant is established by the supplied rooms/council.js: run() throws and escalates on any rejection, quorum failure, or verification non-approval, ending the grant, so the existence of this later complete proposal proves the Step 5 proposal was approved and verified earlier in the run; per the agreement the superseded arch-9 Proposal 2 is not evidence of anything pending. No authority is widened: the plan states metrics derive from records and never grant authority, all steps are described as agreed advisory designs, and the host adds constraints, never permissions. No agreement violation, no source misstatement, no authority widening found.
