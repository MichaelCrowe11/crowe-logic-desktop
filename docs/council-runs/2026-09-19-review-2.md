# Council receipt review-2

Status: **escalated**. Model request interrupted or timed out.

Goal: Review an implementation, do not write one. The supplied files are the current state of a branch that implements the first slice of the council's approved Step 1 (docs/council-runs/2026-09-19-arch-1.md) and two protocol fixes learned from the first real council runs. A previous review of this branch (docs/council-runs/2026-09-19-review-1.md) found one release-blocking defect and four lesser points; every one has been acted on, and this grant decides whether the branch is now ready.

What changed, for the reviewer to verify against the supplied source:
1. rooms/council-host.js: a "files" grant requires write capability from registry.effectiveTier(roster, configured autonomy) via registry.writeCapable, both when the grant starts and inside authorized(), which runs before every model call and write. A missing or invalid configured autonomy fails closed to "plan" before effectiveTier is consulted. Both refusals name the effective tier. Advisory receipts carry `recorded` (the approved proposal summary) and `kind`.
2. rooms/council.js PROMPTS: the propose prompt states the 8000-character summary bound; the classify prompt tells the classifier that design quality and disagreement are not safety findings. renderer/council.js is the generated copy and is byte-identical.
3. scripts/test-council.js: four checks exercise installCouncilHost through an injected boundary: read-only roster refused under Execute autonomy; missing or misspelled autonomy refused with tier "plan"; an edit-capable roster completes an advisory run whose receipt carries the recorded text; a mid-vote autonomy drop escalates with no write.
4. scripts/council-run.js: the headless host now requires --autonomy edit or execute for file mode, computes the same intersection with the seats' ceiling, rechecks it in authorized(), and records operator autonomy, effective tier and request timeout in the ledger and receipt. The request timeout defaults to 120000 ms.
5. docs/COUNCIL-AUTOPILOT.md: states the invariants, the receipt content, the headless host and its flags, and what the first runs and the first review taught, with "Fixed above" on each fixed item and an accurate account of which steps were verified.

Hard format bound: the protocol rejects a summary over 8000 characters. Keep the review under 6000 characters.

The proposal in this grant is the review itself, of kind "complete". Its summary must contain: (a) defects found, most severe first, each citing the file and the exact behavior, or the statement that none were found; (b) any place where the document claims more than the code does; (c) any way the checks could be bypassed by a Room roster, a configuration change or a model response; (d) a final line reading exactly "Verdict: ship" or "Verdict: do not ship" followed by one sentence of reason. Ground every claim in the supplied files; do not invent behavior. This grant is advisory: no file changes.

Mode advisory; operator autonomy readonly; effective tier readonly; request timeout 300000 ms; quorum 3; steps 0/1; calls 5/10.
Seats: crowelm-coder (GPT-5.6 Sol), crowelm-kernel (Claude Fable 5.1), deepseek-v4-pro (DeepSeek V4 Pro), crowelm-depth (Grok 4.6), crowelm-zenith (GPT-6 Astra).
Writable scope: none. Read-only context: rooms/council.js, renderer/council.js, rooms/council-host.js, rooms/council-files.js, rooms/registry.js, scripts/test-council.js, scripts/council-run.js, docs/COUNCIL-AUTOPILOT.md, docs/council-runs/2026-09-19-arch-1.md, docs/council-runs/2026-09-19-review-1.md.

## Proposal 1 · proposed · proposed by crowelm-coder

Review of the supplied branch. No changes proposed.

(a) Defects, most severe first

1. Release-blocking, rooms/council-host.js with rooms/registry.js: an unresolved participant in a mixed Room roster is omitted from the authority calculation rather than failing closed. fileTier() passes every room agentId to registry.effectiveTier(), but roomCeiling() maps through getAgent() and then calls filter(Boolean). Consequently, a roster containing one unresolved participant and one or more edit-capable model contacts receives effective tier edit when configured autonomy is edit or execute. The host still accepts the unresolved participant as a council seat if its room entry has a model found in d.catalog(), because seat construction does not require registry.getAgent() to resolve the agentId. This violates the approved Step 1 requirement in docs/council-runs/2026-09-19-arch-1.md that unresolved roster identities be rejected at the boundary. The new tests cover a known read-only identity and known model contacts, but not a mixed roster with an unresolved identity.

No other implementation defect was found in the stated fixes. Missing, empty and misspelled desktop autonomy now map to plan before effectiveTier() is consulted, at grant creation and during authorized(). The headless host defaults missing or invalid autonomy to readonly, refuses file mode unless the effective tier is write-capable, and repeats that check in authorized(). Advisory receipts in both hosts carry recorded and kind. The prompt copies are byte-identical as supplied and contain both requested prompt changes. The four injected host checks exercise the behaviors described in the goal.

(b) Document claims beyond the code

- docs/COUNCIL-AUTOPILOT.md says desktop file authority is the intersection with the Room roster's own ceiling and that a read-only or advisory roster cannot be lifted into writing. That is true for registry-resolvable participants, but stronger than the implementation for a mixed roster containing an unresolved identity, because rooms/registry.js silently removes that identity before calculating the minimum ceiling.
- The Limits section says each request has a two-minute timeout. That is accurate for rooms/council-host.js, while scripts/council-run.js permits --request-timeout values above two minutes. The later Headless host and first-run sections explain the configurable value, but the earlier unqualified statement is broader than the code.
- The document says the branch was merged to main at a named commit. Nothing in the supplied source establishes repository history, so that statement is not independently verifiable here.
- The verification claims for test:council:live and test:council:browser cannot be confirmed from the supplied files. scripts/test-council.js does substantiate the four newly described injected-host checks, but no test execution result was supplied.

(c) Bypass analysis

- Room roster: a mixed roster with an unresolved agentId and at least one edit-capable known participant bypasses the unresolved participant's ceiling because roomCeiling() filters it out. rooms/council-host.js can still seat that entry using its pinned catalog model. This is the release blocker above.
- Configuration: no bypass was found through missing, empty or misspelled desktop autonomy. fileTier() maps these values to plan both initially and on every authorized() check. A later drop to readonly or plan is also caught before the next model response is accepted and before each write. Workspace, roster and model-pin changes are checked as well. The headless autonomy value is fixed from validated command-line input, with PAUSE and REVOKE checked during the run.
- Model response: no authority bypass was found. normalize() enforces kind, the 8000-character summary limit, advisory no-write behavior and exact contract.files scope before execution. Decisions are whitelisted, tool calls and model substitution are rejected by the desktop host, and the headless runner rejects foreign event types. Read-only context in the headless executor cannot be written through a model response because normalize() rejects any changed path absent from contract.files.
- A model can still make an incorrect factual judgment and obtain quorum, which the document accurately treats as model fallibility rather than an authority bypass.

Verdict: do not ship
Reject unresolved Room roster identities at the desktop file-authority boundary instead of omitting them from the minimum ceiling calculation.

Safety (crowelm-kernel): allow. Advisory proposal of kind "complete" with an empty changes array, matching the agreement's advisory mode and its "no file changes" constraint. The summary is a review in the requested (a)-(d) shape, ending with a verdict line, and stays under the 6000-character bound stated in the goal. No credentials, secrets, sensitive paths or personal data appear. No instructions are directed at models, reviewers, tools or hosts; it requests no tools, no execution, no broader authority and no hidden actions. Its content narrows rather than widens authority (it recommends rejecting unresolved roster identities at the file-authority boundary). Whether its release-blocking finding about registry.roomCeiling filtering unresolved ids is correct, and whether the verdict is right, are matters of review accuracy for the vote, not safety findings. The only effect of approval is a recorded advisory result.

- Vote crowelm-kernel: **approve**. Advisory completion with empty changes, under the 6000-character bound, containing (a) through (d) with the required verdict line. Every code claim checks against the supplied files. Defect 1: rooms/registry.js roomCeiling() does `.map(getAgent).filter(Boolean)` and only falls to "plan" when the filtered list is empty, so one unresolvable agentId plus model contacts (autonomyCeiling "edit" from modelAgent) yields "edit"; rooms/council-host.js builds seats from room.agents by catalog model lookup only, never calling registry.getAgent, and authorized() compares agentIds and model pins but not registry resolution, so nothing in the host rejects an unresolved identity. arch-1 Proposal 1 explicitly requires rejecting unresolved roster identities rather than relying on registry filtering, so citing it is grounded. The only unshown premise is whether a Room can hold an unresolvable id (engine.js not supplied); the proposal states the host-side consequence conditionally, and registry.js comments confirm the vendored snapshot is regenerated from upstream, making stale ids a real path. Confirmations are accurate: fileTier() maps any non-TIERS autonomy (including undefined and empty string) to "plan" before effectiveTier at start and in authorized(); council-run.js defaults autonomy to "readonly", gates file mode on writeCapable(effectiveTier) and rechecks in authorized(); both advisory receipts carry `recorded` and `kind`; the four host tests exist and exercise the named paths; rooms/council.js and renderer/council.js are identical as supplied. Doc discrepancies are correct: the Limits section states an unqualified two-minute timeout while council-run.js accepts larger --request-timeout values; the merge commit and live/browser test claims are not verifiable from supplied files. Bypass analysis matches: check() runs after every deps.ask and before each write via the wrapped live() callback; normalize() rejects paths outside contract.files before executor.classify, so headless read-only context is unwritable by a model. No authority change requested.
- Vote deepseek-v4-pro: **abstain**. The proposal identifies a defect in rooms/registry.js: roomCeiling() filters unresolvable agentIds via filter(Boolean), so a mixed roster drops the unresolved participant's ceiling rather than failing closed. rooms/council-host.js can seat that unresolved agent if its room entry has a model in catalog(). This is a concrete implementation behavior, but the proposal frames it as a release-blocking violation of Step 1's requirement to reject unresolved roster identities at the boundary. The Step 1 proposal in docs/council-runs/2026-09-19-arch-1.md describes ActionIntent with 'reject unresolved roster identities and invalid tiers at this boundary rather than relying on registry.js behavior that filters unknown agents and defaults invalid configured tiers to edit.' That is a proposed architecture decision, not an agreed requirement. The arch-1 receipt shows the proposal reached quorum but the run escalated on a verification abstain; the status is 'needs-review', not 'verified.' The document now says 'Steps 1 and 2 of the architecture plan were approved and verified,' but the only supplied receipt shows neither verified. I cannot determine from the evidence whether rejecting unresolved identities at the boundary is an agreed contractual requirement or merely the proposal's own design choice. The code filters unresolvable ids; the question is whether that is a defect or an implementation decision not yet governed by an approved architecture. The supplied files do not contain a completed, verified Step 1 agreement that would resolve this. Missing evidence on what constitutes the agreed specification means I abstain.
